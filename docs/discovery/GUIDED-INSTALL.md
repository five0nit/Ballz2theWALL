# Guided installation base selection

Target: double-click installation and native macOS/Windows permission guidance with one simple ON/OFF flow, extending the canonical Python runtime controller; no manual MCP/configuration project.

## Evidence
- Brief2Ship 0.8.0 initial receipt: `/tmp/brief2ship-preflight-guided-setup-NUobr0/discovery.json`. Partial/inconclusive (exit 5): local query empty; public matches not suitable evidence of OS permission onboarding.
- Refined local receipt: `/tmp/brief2ship-preflight-ballz-native-HrUjaC/discovery.json`. Discovery complete, decision inconclusive (exit 5): MIT manual review, OSV/package-version evidence, sandbox execution outstanding.
- Manual canonical inspection: README, pyproject.toml, LICENSE, cli.py, adapters.py, store.py; MIT body and copyright reviewed. Existing authored source tests: 403 passed in 4.16s. This is a canonical extension, not execution of an unknown candidate.

## Dispositions (fit / maintainability / dependency cost / platform fit, each /5)
- Ballz2theWALL 0.1.1 at ee566ac, local canonical repository: **selective-reuse**, 5/5/5/4. Keep adapters, reversible receipts, native doctor. Add onboarding and repair Windows transactions. No fork/new harness. Windows storage gap is explicit implementation work.
- Astral uv 0.12.5, https://github.com/astral-sh/uv/releases/tag/0.12.5: **use-as-library** (packaged bootstrap binary), 5/5/5/5. Official versioned release archives support Windows x64/ARM64 and macOS Intel/Apple Silicon. Pin artifact SHA-256; install a private Python environment, do not require Python/git/Homebrew from users. License MIT/Apache-2.0. Documentation inspected at https://docs.astral.sh/uv/getting-started/installation/ and https://docs.astral.sh/uv/concepts/python-versions/.
- trycua/cua libs/cua-driver at 05a0358f9d121341209e31c8a892bda6e1d7c525: **selective-reuse**, 4/4/3/4. Native permission-attribution reference and future desktop execution integration; not a complete installer or runtime/OS privilege grant.
- ansys-saf-desktop-installer 1.16.0: **reject** for this scope, 1/3/2/2. Framework-specific desktop installer, no verified TCC/native agent contract; six extra dependencies. No source executed.
- agent-forge-installer 0.4.99: **reject**, 1/3/3/2. Different agent platform and account/task bootstrap. No verified macOS permission flow.
- aclvl-permissions 0.3.0: **reject**, 0/2/3/0. Django permissions, not OS permissions.

Manual disposition resolves canonical base selection, not Brief2Ship's inconclusive report. Preserve missing OSV evidence and native macOS execution as outstanding; do not claim upstream sandbox tests or Mac permissions passed.

Implementation acceptance: actual local install/CLI round trip, Windows native parsing/installation/ON-OFF isolated tests; Mac static checks plus explicit native-device pending gate. No live agent authentication/profile writes during verification. No publication.
